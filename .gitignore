# Device / bridge secrets -- never commit
firmware/config.h
ingest/.env
frontend/.env

# Ingest bridge dead-letter log (local runtime artifact)
ingest/dead_letter.ndjson

# PlatformIO
.pio/
.pioenvs/
.piolibdeps/

# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/

# Seeded design canvas -- ~2.5 MB of bundled editor, regenerated from the
# .dc.html artboards + canvas.json alongside it. Source is tracked; this
# output is not.
design/railtech-dashboard.html

# Editor / OS
.DS_Store
*.swp
